import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const sb = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function main() { const { data, error } = await sb.from('inventory_items').upsert({ company_id: '8bbd3984-3b72-424b-9c3d-aad1d854dfd4', codigo: 'TEST', nome: 'Test', tipo: 'vidro', quantidade_disponivel: 1, quantidade_reservada: 0, quantidade_minima: 0, unidade: 'm2' }, { onConflict: 'company_id,codigo' }).select(); console.log('ERROR:', error); } main();
