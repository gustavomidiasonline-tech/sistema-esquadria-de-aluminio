import { AppLayout } from '@/components/AppLayout';
import { SocialInbox } from '@/components/redes-sociais/SocialInbox';

const RedesSociais = () => {
  return (
    <AppLayout>
      <SocialInbox />
    </AppLayout>
  );
};

export default RedesSociais;
