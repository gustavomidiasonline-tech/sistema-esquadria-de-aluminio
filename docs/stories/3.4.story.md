# Story 3.4 — RLS Completo para Todas as Tabelas

**Epic:** EPIC-1 Refatoracao SaaS Esquadrias
**Status:** Done
**Agente:** @dev
**Prioridade:** Alta (Segurança)

---

## Objetivo

Corrigir gap de segurança identificado no QA Gate da Story 3.1: tabelas com `company_id` que não tinham políticas RLS. Garantir isolamento multitenancy para todas as tabelas do sistema.

## Acceptance Criteria

- [x] AC1: RLS habilitado e policies criadas para `fornecedores`
- [x] AC2: RLS habilitado e policies criadas para `produtos`
- [x] AC3: RLS habilitado e policies criadas para `servicos`
- [x] AC4: RLS habilitado e policies criadas para `planos_de_corte`
- [x] AC5: RLS habilitado e policies criadas para `agenda_eventos`
- [x] AC6: Policies de DELETE restritas a role `admin`
- [x] AC7: Padrão consistente com migration anterior (companies, clientes, orcamentos, pedidos)

## File List

- `supabase/migrations/20260315040000_rls_complete.sql` (novo)

## Dev Agent Record

### Completion Notes
- 5 tabelas receberam RLS completo: fornecedores, produtos, servicos, planos_de_corte, agenda_eventos
- Cada tabela: SELECT/INSERT/UPDATE para `get_user_company_id()` + DELETE restrito a `has_role(auth.uid(), 'admin'::user_role)`
- Padrão idêntico ao da migration `20260315010000_companies_multitenancy.sql`
- Segurança: nenhum dado de uma empresa pode ser acessado por outra empresa

---

## QA Results

**Agente:** Quinn (@qa)
**Data:** 2026-03-15
**Veredicto:** ✅ PASS

Migration implementa corretamente o padrão RLS estabelecido nas migrations anteriores. Todas as 5 tabelas com isolamento por `company_id`. DELETE restrito a admin — proteção adequada.
